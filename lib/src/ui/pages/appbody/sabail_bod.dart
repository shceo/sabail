import 'package:flutter/material.dart';

class SabailBody extends StatelessWidget {
  const SabailBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}